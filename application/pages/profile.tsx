// - List the Donation NFTs that the donor has received.
// - Display the donation amount, date, and charity name associated with each NFT.

import type { NextPage } from "next";
import Nav from '../components/nav';

const Profile: NextPage = () => {
  return (
    <div>
      <Nav/>
    </div>
  );
};

export default Profile;
